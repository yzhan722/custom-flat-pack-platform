{
  "name": "@cfp/core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic engineering kernel: catalog, design spec, rules, panel compiler, BOM, pricing, assembly, packaging, production release, order state machines.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
